@model IEnumerable<WebSiteVozesUnidas.Models.VagaEmprego>

@{
    ViewData["Title"] = "userCandidatura";
}

<h1>userCandidatura</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cargo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResumoVaga)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Foto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HorarioExpediente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Beneficios)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Requisitos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoVaga)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cargo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResumoVaga)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Foto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioExpediente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Beneficios)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Requisitos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoVaga)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdVagaEmprego">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdVagaEmprego">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdVagaEmprego">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
 *@

<div class="d-flex">
    @foreach (var item in Model)
    {
        var fotoVaga = "default.jpg";

        @*  if (item.Foto != null)
    {
    fotoVaga = item.Foto;
    } *@

        <div class="col-3 d-flex flex-column m-3 border border-2 border-dark rounded shadow">
            <div>
                <img src="~/img/@fotoVaga" class="h-100 w-100 border-bottom border-2 border-dark" />
            </div>
            <div>
                <p class="fw-bold">Cargo:</p>
                <p>@item.Cargo</p>
                <p class="fw-bold">Resumo:</p>
                <p>@item.ResumoVaga</p>
                <p class="fw-bold">Descrição:</p>
                <p>@item.DescricaoVaga</p>
            </div>
            <div class="d-flex justify-content-end mt-auto">
                <form method="post" asp-action="Create" asp-controller="CandidatoVagas" class="col-6">
                    <input type="hidden" name="IdVaga" value="@item.IdVagaEmprego" />

                    <button type="submit" class="btn btn-success rounded-0 col-12">Candidatar</button>
                </form>
                <form class="col-6">
                    <button asp-action="Details" asp-route-id="@item.IdVagaEmprego" class="btn btn-info text-white rounded-0 col-12">Informações</button>
                </form>
            </div>
        </div>
    }
</div>