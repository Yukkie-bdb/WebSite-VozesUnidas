@model IEnumerable<WebSiteVozesUnidas.Models.LikesPost>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLikesPost">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLikesPost">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLikesPost">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
